# Интеграция Telegram бота с сайтом Pirats.studio

## Обзор

Система интеграции позволяет получать заявки с сайта прямо в Telegram администратору.

### Компоненты системы:

1. **telegram_bot.py** - Python-сервер, который принимает заявки и отправляет их в Telegram
2. **chat-bot.js** - Фронтенд чат-бот для диалога с посетителями
3. **ajax.php** - Обработчик форм на сайте
4. **standart.php** - Главная страница с формами

---

## Как это работает

### 1. Заявки с форм сайта

Когда пользователь заполняет форму на сайте (модальное окно "Остался шажочек до мечты!"):

```
Пользователь заполняет форму
    ↓
script.js отправляет данные в ajax.php
    ↓
ajax.php сохраняет в БД и отправляет в Telegram бота
    ↓
telegram_bot.py (/webhook/application) получает данные
    ↓
Сообщение отправляется администратору в Telegram
```

**Формат данных от формы:**
```json
{
  "name": "Иван Иванов",
  "phone": "+7 900 123-45-67",
  "email": "ivan@example.com",
  "service_type": "Заявка с формы сайта",
  "application_id": "FORM_123",
  "conversation_summary": "Клиент оставил заявку через всплывающую форму на сайте"
}
```

**Сообщение в Telegram:**
```
🎯 НОВАЯ ЗАЯВКА С ФОРМЫ
━━━━━━━━━━━━━━━━━━━━

👤 Имя: Иван Иванов
📞 Телефон: +7 900 123-45-67
📧 Email: ivan@example.com
🎨 Услуга: Заявка с формы сайта

🕐 Время: 25.10.2025 15:30:45
```

---

### 2. Заявки из чат-бота

Когда пользователь общается с чат-ботом через кнопку "Обсудить проект":

```
Пользователь открывает чат
    ↓
Бот задает вопросы и собирает данные (имя, телефон, email, тип сайта)
    ↓
После завершения диалога chat-bot.js отправляет данные
    ↓
telegram_bot.py (/webhook/chat) получает данные
    ↓
Сообщение отправляется администратору в Telegram
```

**Формат данных от чат-бота:**
```json
{
  "name": "Мария Петрова",
  "phone": "+7 900 987-65-43",
  "email": "maria@example.com",
  "website_type": "Лендинг",
  "additional_questions": [
    "Сколько стоит разработка?",
    "Какие сроки?"
  ]
}
```

**Сообщение в Telegram:**
```
💬 ЗАЯВКА ИЗ ЧАТ-БОТА
━━━━━━━━━━━━━━━━━━━━

👤 Имя: Мария Петрова
📞 Телефон: +7 900 987-65-43
📧 Email: maria@example.com
🎨 Тип сайта: Лендинг

❓ Дополнительные вопросы:
1. Сколько стоит разработка?
2. Какие сроки?

🕐 Время: 25.10.2025 15:35:20
```

---

### 3. Дополнительные вопросы из чата

Если пользователь задает вопросы после заполнения основных данных:

```json
{
  "type": "additional_question",
  "question": "Можете ли вы добавить анимации?",
  "user_data": {
    "name": "Мария Петрова",
    "phone": "+7 900 987-65-43",
    "email": "maria@example.com"
  }
}
```

**Сообщение в Telegram:**
```
❓ ВОПРОС ИЗ ЧАТА
━━━━━━━━━━━━━━━━━━━━

💬 Вопрос:
Можете ли вы добавить анимации?

👤 Имя: Мария Петрова
📞 Телефон: +7 900 987-65-43
📧 Email: maria@example.com

🕐 Время: 25.10.2025 15:36:15
```

---

## Запуск и настройка

### 1. Настройка конфигурации

Отредактируйте файл `config.env`:

```bash
# Токен вашего Telegram бота (получите у @BotFather)
TELEGRAM_BOT_TOKEN=8420622652:AAFJNVkLUNEUJ0OdfHqJJrVXRU_jLQmGuPY

# ID чата администратора (ваш Telegram ID)
TELEGRAM_CHAT_ID=1062366418
```

**Как получить свой Chat ID:**
1. Запустите скрипт: `python get_my_chat_id.py`
2. Напишите боту любое сообщение
3. Скрипт выведет ваш Chat ID

### 2. Запуск Telegram бота

```bash
# Windows
python telegram_bot.py

# Linux/Mac
python3 telegram_bot.py
```

**Бот запустится на порту 5000:**
```
🤖 Telegram Bot для Pirats.studio
📡 Bot Token: 8420622652...
💬 Chat ID: 1062366418
🌐 Сервер работает на: http://localhost:5000
```

### 3. Проверка работоспособности

```bash
# Проверка что бот запущен
curl http://localhost:5000/health

# Ответ:
# {"status": "ok", "bot": "running", "timestamp": "2025-10-25T15:30:00"}
```

### 4. Тестирование отправки заявок

```bash
# Тест заявки с формы
curl -X POST http://localhost:5000/webhook/application \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый пользователь",
    "phone": "+7 900 123-45-67",
    "email": "test@example.com",
    "service_type": "Тестовая заявка"
  }'

# Тест заявки из чата
curl -X POST http://localhost:5000/webhook/chat \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Чат тестер",
    "phone": "+7 900 987-65-43",
    "email": "chat@example.com",
    "website_type": "Лендинг"
  }'
```

---

## API Endpoints

### POST /webhook/application
Принимает заявки с форм сайта

**Параметры:**
- `name` (string) - Имя клиента
- `phone` (string) - Номер телефона
- `email` (string, optional) - Email адрес
- `service_type` (string, optional) - Тип услуги
- `note` (string, optional) - Заметка
- `application_id` (string, optional) - ID заявки

**Ответ:**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена администратору"
}
```

### POST /webhook/chat
Принимает заявки из чат-бота

**Параметры:**
- `name` (string) - Имя клиента
- `phone` (string) - Номер телефона
- `email` (string, optional) - Email адрес
- `website_type` (string, optional) - Тип сайта
- `additional_questions` (array, optional) - Дополнительные вопросы
- `type` (string, optional) - Тип сообщения ('application' или 'additional_question')

**Для дополнительного вопроса:**
- `type` = 'additional_question'
- `question` (string) - Текст вопроса
- `user_data` (object) - Данные пользователя

**Ответ:**
```json
{
  "success": true,
  "message": "Заявка отправлена администратору"
}
```

### GET /health
Проверка работоспособности бота

**Ответ:**
```json
{
  "status": "ok",
  "bot": "running",
  "timestamp": "2025-10-25T15:30:00"
}
```

---

## Логирование

Бот логирует все события в файл `bot.log`:

```
2025-10-25 15:30:00 - INFO - Конфигурация загружена из config.env
2025-10-25 15:30:01 - INFO - Соединение с Telegram установлено успешно
2025-10-25 15:30:01 - INFO - Бот запущен и ожидает заявок...
2025-10-25 15:35:20 - INFO - Получена заявка с формы: Иван Иванов
2025-10-25 15:35:21 - INFO - Сообщение отправлено в Telegram
```

---

## Устранение неполадок

### Бот не отправляет сообщения в Telegram

1. Проверьте правильность токена и Chat ID в `config.env`
2. Убедитесь что вы написали боту хотя бы одно сообщение
3. Проверьте логи в `bot.log`

### Формы не отправляют данные в бота

1. Убедитесь что `telegram_bot.py` запущен
2. Проверьте что сайт использует правильный URL: `http://localhost:5000/webhook/application`
3. Проверьте что порт 5000 не занят другим приложением

### Чат-бот не работает

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь что файл `chat-bot.js` подключен к странице
3. Проверьте что URL `http://localhost:5000/webhook/chat` доступен

---

## Автозапуск бота

### Windows (PowerShell)

Создайте скрипт `start_bot.ps1`:
```powershell
cd "C:\Users\sofia\Music\Pirates\pirates"
python telegram_bot.py
```

Добавьте в автозагрузку или запускайте при старте системы.

### Linux (systemd)

Создайте файл `/etc/systemd/system/pirates-bot.service`:
```ini
[Unit]
Description=Pirates Studio Telegram Bot
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/pirates/pirates
ExecStart=/usr/bin/python3 telegram_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable pirates-bot
sudo systemctl start pirates-bot
```

---

## Улучшения и доработки

### Возможные улучшения:

1. **Добавить кнопки в Telegram** - для быстрых действий (позвонить, написать email)
2. **Уведомления о новых сообщениях** - звук при получении заявки
3. **Статистика** - количество заявок за день/неделю/месяц
4. **CRM интеграция** - автоматическое создание сделок в CRM
5. **Email уведомления** - дублирование заявок на email

---

## Контакты

При возникновении вопросов обращайтесь:
- Telegram: @startupus
- Телефон: +7 909 150-34-44
- Сайт: https://pirats.studio

